## Crew · {{NAME}}

This repo has a **{{NAME}}** crew member — {{ROLE}}. REPLACE this sentence with the one rule every
session should always know: **when** to delegate to the `{{NAME}}` agent, and the boundary it keeps
(e.g. it only proposes; fixes still go through capture → compile → review). Keep this brief tight —
it loads into **every** session while the member is enabled, so it costs context. One short
paragraph, no more.
