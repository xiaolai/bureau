# internal development notes — not shipped
/dev-docs/

# the gazette (the rendered board, derived from bureau/ — rebuild with bureau:inspect)
/bureau/gazette/

# recursion-engine derived cache (regenerable by `gazette fsck`; never committed)
/.bureau-cache/
